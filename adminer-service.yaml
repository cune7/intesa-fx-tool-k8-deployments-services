apiVersion: v1
kind: Service
metadata:
  name: intesa-fx-adminer
  namespace: fx-tool
spec:
  selector:
    app: intesa-fx-adminer
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
  type: ClusterIP
